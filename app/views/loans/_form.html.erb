<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :principal %>
    <%= form.text_field :principal, id: :loan_principal %>
  </div>

  <div class="field">
    <%= form.label :months %>
    <%= form.number_field :months, id: :loan_months %>
  </div>

  <div class="field">
    <%= form.label :years %>
    <%= form.number_field :years, id: :loan_years %>
  </div>

  <div class="field">
    <%= form.label :interest %>
    <%= form.text_field :interest, id: :loan_interest %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
